let salaryRate = 80;
let hoursWorked = 40;
let hoursPerDay = 5;
let daysLimit = 11 - 2;
let daysPerWeek = 5;
let workingTimeLimit = hoursPerDay * daysPerWeek;

console.log((daysLimit * hoursPerDay) > hoursWorked);
console.log(salaryRate * hoursWorked);

